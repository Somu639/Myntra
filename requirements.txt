# Streamlit Cloud / dashboard only.
# Collector + LLM packages live in requirements-collect.txt (local pipeline).
# Do not add app-store-scraper here — it hard-pins requests==2.23.0 and
# Streamlit Cloud's resolver (uv + pip) cannot override that pin.
streamlit>=1.40
pandas>=2.0
python-dotenv>=1.0.1
