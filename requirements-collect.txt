# Local collection / extraction extras (not used by Streamlit Cloud).
# app-store-scraper is omitted on purpose: it hard-pins requests==2.23.0.
# collect.py uses Apple's iTunes RSS feed for App Store reviews instead.
-r requirements.txt
google-play-scraper>=1.2.7
praw>=7.7.1
youtube-comment-downloader>=0.1.76
anthropic>=0.40.0
requests>=2.32
urllib3>=2
